import { Box } from '@mui/material';

const Navbar = () => {
    return (
        <Box className="h-20 absolute top-0 right-0 left-0 shadow-sm">
            Navbar
        </Box>
    );
};

export default Navbar;
